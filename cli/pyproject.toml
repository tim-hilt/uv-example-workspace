[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "core",
    "native",
]

[tool.uv.sources]
core = { workspace = true }
native = { workspace = true }
